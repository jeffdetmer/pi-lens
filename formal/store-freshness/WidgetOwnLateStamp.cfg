\* expect: pass
\* module: StoreFreshness
\* The code (#3503): widget-state observedAt is the pipeline's analysis read time, taken before each read of the analysed bytes (clients/pipeline.ts analysisReadAtMs) and passed to recordDiagnostics; reconcileStaleWidgetFiles compares the file's mtime against it. The same constants model the workspace-diagnostics cache's dependency axis (#3505): the entry's scannedAt is taken per file before its read and isEntryFresh compares each import's mtime against it, unlatched. Was violated; the pre-fix stamp is MutWidgetOwnLateStamp.
CONSTANTS
    Writes = 2
    SizeMode = "any"
    MaxTime = 6
    Tol = 2
    Gran = 1
    Skew = 1
    MinReadDelay = 0
    Stamp = "start"
    Gate = "mtime"
    ForceHash = FALSE
    SizeOnly = FALSE
    StatAt = "before"
    SizeKey = TRUE
    Binding = FALSE
    BindingAt = "read"
    MaxServes = 2
    Latch = FALSE
    AllowRegress = FALSE
    ServerLag = FALSE
SPECIFICATION Spec
INVARIANTS NoStaleServeBeyondTol NoSpuriousDemotion
CHECK_DEADLOCK FALSE
