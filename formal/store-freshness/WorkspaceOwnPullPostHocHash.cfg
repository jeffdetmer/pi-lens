\* expect: violated NoStaleServe
\* module: StoreFreshness
\* Still the code (#3505 part b, deferred): the pull path's full report hashes the disk AFTER the server answered (clients/lsp/client.ts clientRequestWorkspaceDiagnostics), and runWorkspaceDiagnostics stats after; both describe the post-edit bytes, so the binding cannot refuse the serve. FixWorkspacePullBinding is the checked fix.
CONSTANTS
    Writes = 2
    SizeMode = "any"
    MaxTime = 6
    Tol = 2
    Gran = 1
    Skew = 1
    MinReadDelay = 0
    Stamp = "end"
    Gate = "eq"
    ForceHash = FALSE
    SizeOnly = FALSE
    StatAt = "after"
    SizeKey = TRUE
    Binding = TRUE
    BindingAt = "record"
    MaxServes = 2
    Latch = FALSE
    AllowRegress = FALSE
    ServerLag = FALSE
SPECIFICATION Spec
INVARIANTS NoStaleServe
CHECK_DEADLOCK FALSE
