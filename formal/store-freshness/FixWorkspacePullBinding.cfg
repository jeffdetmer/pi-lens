\* expect: pass
\* module: StoreFreshness
\* The checked fix for the pull path, not yet in the code (#3505 part b): persist a pull answer only with a binding to bytes pi-lens SENT the server (the push binding's sentHash); otherwise do not cache it.
CONSTANTS
    Writes = 2
    SizeMode = "any"
    MaxTime = 6
    Tol = 2
    Gran = 1
    Skew = 1
    MinReadDelay = 0
    Stamp = "end"
    Gate = "eq"
    ForceHash = FALSE
    SizeOnly = FALSE
    StatAt = "after"
    SizeKey = TRUE
    Binding = TRUE
    BindingAt = "read"
    MaxServes = 2
    Latch = FALSE
    AllowRegress = FALSE
    ServerLag = TRUE
SPECIFICATION Spec
INVARIANTS NoStaleServe
CHECK_DEADLOCK FALSE
