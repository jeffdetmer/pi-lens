\* expect: violated NoStaleServeBeyondTol
\* module: StoreFreshness
\* Mutant (the code before #3503): inline-blocker import axis with recordedAtMs = Date.now() in recordInlineBlockers, called after runPipeline returns; detectDrift compares each forward import against it. Dependency-drift demotion latches.
CONSTANTS
    Writes = 2
    SizeMode = "any"
    MaxTime = 6
    Tol = 2
    Gran = 1
    Skew = 1
    MinReadDelay = 0
    Stamp = "end"
    Gate = "mtime"
    ForceHash = FALSE
    SizeOnly = FALSE
    StatAt = "before"
    SizeKey = TRUE
    Binding = FALSE
    BindingAt = "read"
    MaxServes = 2
    Latch = TRUE
    AllowRegress = FALSE
    ServerLag = FALSE
SPECIFICATION Spec
INVARIANTS NoStaleServeBeyondTol
CHECK_DEADLOCK FALSE
