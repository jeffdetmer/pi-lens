\* expect: violated NoStaleServeBeyondTol
\* module: StoreFreshness
\* Mutant (the code before #3503): widget-state observedAt = Date.now() at recordDiagnostics, after the pipeline read and the dispatch await; an external write landing during the dispatch is older than the stamp. The same constants are the workspace cache's pre-#3505 dependency axis (scannedAt = Date.now() in record(), after the whole sweep).
CONSTANTS
    Writes = 2
    SizeMode = "any"
    MaxTime = 6
    Tol = 2
    Gran = 1
    Skew = 1
    MinReadDelay = 0
    Stamp = "end"
    Gate = "mtime"
    ForceHash = FALSE
    SizeOnly = FALSE
    StatAt = "before"
    SizeKey = TRUE
    Binding = FALSE
    BindingAt = "read"
    MaxServes = 2
    Latch = FALSE
    AllowRegress = FALSE
    ServerLag = FALSE
SPECIFICATION Spec
INVARIANTS NoStaleServeBeyondTol
CHECK_DEADLOCK FALSE
