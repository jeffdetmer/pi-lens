\* expect: pass
\* module: StoreFreshness
\* The code (#3505, per-file path): runWorkspaceDiagnostics stats each file before the read its answer is computed from (the pre-open pass hands its stat on with its content; processFile stats before its own read). Was violated; the pre-fix order is MutWorkspaceOwnStatAfterRead. The pull path is WorkspaceOwnPullPostHocHash, still violated.
CONSTANTS
    Writes = 2
    SizeMode = "any"
    MaxTime = 6
    Tol = 2
    Gran = 1
    Skew = 1
    MinReadDelay = 0
    Stamp = "end"
    Gate = "eq"
    ForceHash = FALSE
    SizeOnly = FALSE
    StatAt = "before"
    SizeKey = TRUE
    Binding = FALSE
    BindingAt = "read"
    MaxServes = 2
    Latch = FALSE
    AllowRegress = FALSE
    ServerLag = FALSE
SPECIFICATION Spec
INVARIANTS NoStaleServe
CHECK_DEADLOCK FALSE
